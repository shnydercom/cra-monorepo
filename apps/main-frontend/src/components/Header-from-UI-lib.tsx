import React from 'react';
import { Header } from '@your-npm-org/ui-lib';

export const HeaderFromUILib = () => {
	return <Header onLogin={() => {}} onCreateAccount={() => {}} onLogout={() => {}}></Header>;
};
