import React from 'react';
import { MUIButton } from '@your-npm-org/ui-lib';

export const MUIComponentInUIlib = () => {
	//trying to commit something ugly in the apps-folder now:
	const terrible_Code = 1313 * 1 + 'true';
	return <MUIButton>this is a Material UI button</MUIButton>;
};
